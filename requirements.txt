# Розпізнавання мови — локально, без ключів і без мережі (крім першого
# завантаження моделі). Ліцензія MIT.
faster-whisper>=1.1.0

# Пошук обличчя через вбудований у macOS Vision. Потрібні саме ці три модулі:
# Vision (детектор), Quartz (читання кадру), Cocoa (базові типи).
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-Cocoa>=10.0

# ffmpeg і ffprobe — усі різи, швидкість, гучність, колір і вимір яскравості.
# Ставиться як звичайний пакет, окремо качати нічого не треба.
static-ffmpeg>=2.7
