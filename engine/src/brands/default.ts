import type {Brand} from './types';

/**
 * Нейтральний бренд «з коробки».
 *
 * Це не чийсь фірмовий стиль, а спокійна основа, з якою ролик виглядає
 * пристойно ще до того, як ти щось налаштуєш. Один акцентний колір,
 * системні шрифти, темне тло — бо графіка найчастіше лягає поверх відео.
 *
 * Щоб зробити свій бренд: скопіюй цей файл під своєю назвою
 * (`src/brands/moye.ts`), поміняй кольори й шрифти, додай його в
 * `src/brands/index.ts` — і він сам зʼявиться у випадайці кожної композиції.
 */
export const brandDefault: Brand = {
  id: 'default',
  name: 'Базовий',
  fonts: {
    // Системні родини: нічого не треба качати, працює й у Studio, і в рендері.
    display: 'Georgia, "Times New Roman", serif',
    body: '"Helvetica Neue", Arial, sans-serif',
  },
  colors: {
    bg: '#101010',
    panel: '#1A1A1A',
    text: '#F5F5F5',
    textMuted: '#8A8A8A',
    // Глибокий сливовий. На світлому кадрі читається чудово (13:1),
    // на темному його підміняє accentSoft — див. коментар нижче.
    accent: '#660033',
    // Той самий колір, освітлений до робочого контрасту на темному тлі
    // (4.4:1 проти майже чорного). Композиція субтитрів бере його сама,
    // коли бачить, що кадр під написом темний.
    accentSoft: '#E02985',
  },
};
